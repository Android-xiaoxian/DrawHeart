<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="fsx.heart.drawheart">

    <application
        android:name=".base.BaseApplication"
        android:allowBackup="true"
        android:anyDensity="true"

        android:hardwareAccelerated="true"
        android:label="@string/app_name"
        android:launchMode="singleInstance"
        android:requestLegacyExternalStorage="true"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:usesCleartextTraffic="true">


        <activity android:name="fsx.heart.drawheart.activity.GuideActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".activity.MainActivity"
            android:screenOrientation="portrait"
            android:theme="@style/AppTheme" />

        <activity
            android:name=".other.activity.VerticalSeekBarActivity"
            android:screenOrientation="portrait" />

        <activity
            android:name=".other.activity.RecyclerViewTestActivity"
            android:screenOrientation="portrait" />

        <activity
            android:name=".other.activity.ImageViewTestActivity"
            android:screenOrientation="portrait" />

        <activity
            android:name=".other.activity.GridViewTestActivity"
            android:screenOrientation="portrait" />


        <meta-data
            android:name="design_width_in_dp"
            android:value="375" />
        <meta-data
            android:name="design_height_in_dp"
            android:value="667" />

        <!-- 全面屏适配去除黑边 -->
        <meta-data
            android:name="max_aspect"
            android:value="3.0" />
        <meta-data
            android:name="android.max_aspect"
            android:value="3.0" />

    </application>

</manifest>